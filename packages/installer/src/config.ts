/**
 * Installer configuration — CDN URLs and version info.
 * Update PAGES_BASE_URL when the GitHub Pages deployment changes.
 */

/** GitHub Pages base URL for this deployment. */
export const PAGES_BASE_URL = 'https://niconicoCip.github.io/Silkweaver/'

/** CDN URL for the compiled engine bundle. */
export const ENGINE_CDN_URL = `${PAGES_BASE_URL}engine.js`

/** Installer version (matches engine version). */
export const INSTALLER_VERSION = '0.1.0'
